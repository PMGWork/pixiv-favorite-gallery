{
  "name": "random-gallery",
  "private": true,
  "version": "1.0.0",
  "description": "Random Gallery - Pixiv/Raindrop favorite viewer",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --filter ./web build",
    "preview": "pnpm -r --filter ./web preview"
  }
}
