{
  "name": "random-gallery",
  "private": true,
  "version": "1.0.0",
  "description": "Random Gallery - Pixiv/Raindrop favorite viewer",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --filter @random-gallery/web build",
    "preview": "pnpm -r --filter @random-gallery/web preview",
    "build:shared": "pnpm -r --filter @random-gallery/shared build"
  }
}
